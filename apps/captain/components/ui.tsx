// Captain re-exports the shared design-system primitives so all screens use a
// single source of truth (@getyourboat/ui). Add app-specific wrappers here only
// when truly captain-specific.
export {
  Input,
  Textarea,
  Select,
  Label,
  Field,
  FieldError,
  Checkbox,
  Spinner,
  Alert,
  Badge,
  FormErrorBanner,
  Modal,
} from "@getyourboat/ui";
