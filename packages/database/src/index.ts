// Prisma client + generated types/enums (for backend infrastructure use only).
export * from "./client.js";

// Repository ports + Prisma-backed implementations (the API depends on these,
// never on the Prisma client directly).
export * from "./repositories/index.js";
